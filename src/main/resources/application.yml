spring:
  datasource:
    url: "jdbc:postgresql://localhost:5431/quendless"
    username: "postgres"
    password: "123456"

  session:
    store-type: jdbc
    jdbc:
      initialize-schema: always
server:
  servlet:
    session:
      timeout: 15m